# Solidity Implements on Foundry

## Proxy
A Logic contract to stock the login function.
A Proxy contract to stock the state and have a delegate call on Logic contract.
A Caller contract to mock the Proxy called.